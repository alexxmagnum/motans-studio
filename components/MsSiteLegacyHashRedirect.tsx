"use client";

/**
 * Fase 1 — los hashes de la landing viven en `/`.
 * Ya no redirigimos /#servicios ni /#contacto a rutas multipágina.
 */
export function MsSiteLegacyHashRedirect(): null {
  return null;
}
