import type React from "react";
import { MsHeroPathsHub } from "./MsHeroPathsHub.js";

/** Rutas de trabajo — viven dentro del card del hero (`MsHeroPathsHub`). */
export function MsStudioPathCards(): React.ReactElement {
  return <MsHeroPathsHub />;
}
