import type React from "react";
import { MsMotanosPlansSection } from "../plans/MsMotanosPlansSection.js";

/** Bloque Planes — ancla #planes en home. */
export function MsHomePlans(): React.ReactElement {
  return <MsMotanosPlansSection variant="home" />;
}
