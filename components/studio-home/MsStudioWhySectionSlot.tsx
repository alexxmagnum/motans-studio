import type { ReactElement } from "react";
import { MsStudioWhySection } from "./MsStudioWhySection.js";

/** Acto 03 — Por qué Motans Studio (Fase 3 editorial). */
export function MsStudioWhySectionSlot(): ReactElement {
  return <MsStudioWhySection />;
}
